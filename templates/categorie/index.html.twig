{% extends 'base.html.twig' %}

{% block title %}Liste des Articles{% endblock %}

{% block body %}
    <h1>Liste des Catégories</h1>
    <div class="container">
        <ul class="list-group ">
            {% for categorie in categories %}
              <a class="list-group-item list-group-item-action list-group-item-dark m-2 badge" href="{{ path('app_categorie_slug',{'slug': categorie.slug}) }}"><p>{{ categorie.titre }}</p></a>
            {% endfor %}
        </ul></div>
{% endblock %}
